# Copy this file, rename the copy to  .env.local  and paste your keys in.
# .env.local stays on this computer and is never uploaded anywhere.

# ---- Supabase (the real Facebook conversations, already collected) ----
SUPABASE_URL=https://qiyfuxiccphxlnrhsmvf.supabase.co

# Get this from: Supabase dashboard > Project Settings > API Keys > service_role
# Paste it after the = sign. Never share this key with anyone.
SUPABASE_SERVICE_KEY=

# ---- Facebook (live messages — the old key expired 14 Aug) ----
FB_PAGE_ID=303915139678621
FB_PAGE_TOKEN=

# ---- Login PIN for the app (set this on Vercel too) ----
APP_PIN=
